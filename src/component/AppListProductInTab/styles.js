import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  flexBox: {
    flexDirection: 'row',
    flexWrap: 'nowrap',
    alignItems: 'center',
  },

  flexTitle: {
    paddingBottom: 10,
    justifyContent: 'space-between',
  },
});
