import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },

  input: {
    width: '100%',
    height: '100%',
    alignSelf: 'center',

    paddingLeft: 16,
    fontSize: 16,

    borderColor: '#ECEBED',
    borderWidth: 1,
    borderRadius: 4,
  },
});
