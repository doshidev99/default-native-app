import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    marginBottom: 10,
    marginRight: 10,

    borderColor: '#CDCDCD',
    borderWidth: 2,
    borderStyle: 'dashed',
  },

  wrapperImage: {
    width: '100%',
    height: '100%',
    overflow: 'hidden',
  },

});
