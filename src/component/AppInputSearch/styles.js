import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    justifyContent: 'center',
    alignItems: 'center',
  },

  input: {
    width: '100%',
    height: '100%',
    paddingLeft: 41,
    fontSize: 13,

    borderColor: '#ECEBED',
    borderWidth: 1,
    borderRadius: 2,
  },
});
