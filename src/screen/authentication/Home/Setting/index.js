import React, { memo } from 'react';
import {
  Text, View,
} from 'react-native';

import PropTypes from 'prop-types';

const Home = () => (
  <View>
    <Text> Setting </Text>
  </View>
);

Home.propTypes = {
};

Home.defaultProps = {
};
export default memo(Home);
