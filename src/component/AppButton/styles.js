import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    justifyContent: 'center',
    borderRadius: 2,
  },
  text: {
    letterSpacing: 0.5,
  },

  wrapperContent: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
  },

});
